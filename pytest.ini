[pytest]
markers =
    integration: mark tests as integration tests that call external APIs

# Ignore long-running or auxiliary files/directories by default
# - ignore the specific slow test
# - ignore the scripts/ helpers directory
addopts = --ignore=scripts --ignore=backtest

# Common non-recursive dirs (optional)
norecursedirs = .venv build dist
